name: Apply published books layout

on:
  push:
    paths:
      - '.github/workflows/apply-published-books.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update published books section
        run: python3 tools/update_published_books.py
      - name: Commit updated homepage
        run: |
          rm -f .github/workflows/apply-published-books.yml tools/update_published_books.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Unify published books and add cover placeholders"
          git push
